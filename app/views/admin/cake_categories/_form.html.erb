<%= form_for([:admin, @cake_category]) do |f| %>
  <% if @cake_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cake_category.errors.count, "error") %> prohibited this cake_category from being saved:</h2>

      <ul>
      <% @cake_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_name %><br>
    <%= f.text_field :category_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
